{
  "name": "power-watt-34",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "concurrently --raw \"npm run build --prefix client\" \"npm run build --prefix server\"",
    "commit": "bash ./npm-run-commit.sh",
    "pull-force": "bash ./npm-run-pull-force.sh",
    "dev": "concurrently --raw \"npm run dev --prefix client\" \"npm run dev --prefix server\"",
    "dev:client": "npm run dev --prefix client",
    "dev:server": "npm run dev --prefix server"
  },
  "dependencies": {
    "concurrently": "^9.2.1"
  }
}
